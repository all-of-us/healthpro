<div>
    <ul>
        {% for param in params %}
            <li class="btn btn-default btn-xs"><span class="fa fa-close"></span> {{ filterOptions[param] }}</li>
        {% endfor %}
    </ul>
</div>
