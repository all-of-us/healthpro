{% extends 'admin/base.html.twig' %}
{% block title %}Admin - {% endblock %}
{% block body %}
    <ol class="breadcrumb">
        <li><a href="{{ path('home') }}">Home</a></li>
        <li class="active">Admin</li>
    </ol>
    <div class="page-header">
        <h2><i class="fa fa-cog" aria-hidden="true"></i> Admin</h2>
    </div>

    <div class="row home-menu">
        <div class="col-sm-6 col-md-4">
            <a href="{{ path('admin_sites') }}" class="btn btn-lg btn-default btn-block"><i class="fa fa-hospital-o" aria-hidden="true"></i> Site Management</a>
        </div>
        <div class="col-sm-6 col-md-4">
            <a href="{{ path('admin_notifications') }}" class="btn btn-lg btn-default btn-block"><i class="fa fa-envelope" aria-hidden="true"></i> Email Notifications</a>
        </div>
        <div class="col-sm-6 col-md-4">
            <a href="{{ path('admin_notices') }}" class="btn btn-lg btn-default btn-block"><i class="fa fa-info-circle" aria-hidden="true"></i> Page Notices</a>
        </div>
        <div class="col-sm-6 col-md-4">
            <a href="{{ path('admin_feature_notifications') }}" class="btn btn-lg btn-default btn-block"><i class="fa fa-bell" aria-hidden="true"></i> Feature Notifications</a>
        </div>
    </div>

    <div class="text-center small" style="margin-top: 100px;">
        <strong>Debug tools:</strong>
        <a href="{{ path('admin_debug_missing_measurements') }}">Missing Physical Measurements</a> &bull;
        <a href="{{ path('admin_debug_missing_orders') }}">Missing Biobank Orders</a> &bull;
        <a href="{{ path('admin_debug_participants') }}">Participant Summary Lookup</a>
    </div>
{% endblock %}
