<div class="modal fade" id="view_delete_modal" tabindex="-1" role="dialog" aria-labelledby="viewDeleteModal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-body">Are you sure you want to delete this view?</div>
            <div class="modal-footer">
                {{ form_start(workQueueViewDeleteForm, { attr: { action: path('workqueue_view_delete', { viewType:
                    viewType, currentUrl: currentUrl, currentViewId: viewId }) } }) }}
                <button type="submit" class="btn btn-danger">Delete</button>
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                {{ form_end(workQueueViewDeleteForm) }}
            </div>
        </div>
    </div>
</div>
