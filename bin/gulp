#!/bin/bash
#
# Convenience script to run the Composer-installed version of the `node gulp`
# command, passing along all arguments.
#

APP_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/.."
GULP="${APP_DIR}/node_modules/gulp/bin/gulp.js"

node $GULP "$@"
