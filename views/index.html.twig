{% extends 'base.html.twig' %}
{% block body %}
    <div class="page-header">
        <h2>Welcome!</h2>
    </div>
    <p class="text-center">
        <a href="{{ path('participants') }}" class="btn btn-lg btn-default"><i class="fa fa-user" aria-hidden="true"></i> Participant Lookup</a>
        <a href="#" class="btn btn-lg btn-default disabled"><i class="fa fa-medkit" aria-hidden="true"></i> Sample Lookup</a>
    </p>
{% endblock %}
