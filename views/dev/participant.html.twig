{% extends 'base.html.twig' %}
{% block body %}
    <h2>Manually create a participant</h2>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <p><button type="submit" class="btn btn-primary">Create</button></p>
    {{ form_end(form) }}
{% endblock %}
