{% extends 'base.html.twig' %}
{% block body %}
    {% include 'order-header.html.twig' with { active: 'finalize' } %}
    

    {{ form_start(finalizeForm) }}
        {{ form_widget(finalizeForm) }}
        <p>
            <button type="submit" class="btn btn-primary">Save</button>
        </p>
    {{ form_end(finalizeForm) }}
{% endblock %}
