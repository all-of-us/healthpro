{% macro displayDate(order, dateType) %}
    {% if order[dateType] %}
        {{ order[dateType]|date('n/j/Y g:ia', app.userTimezone) }}
    {% else %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
    {% endif %}
{% endmacro %}

{% macro displaySite(siteName, site) %}
    {% if siteName is empty and site is empty %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
    {% else %}
        {{ siteName ? siteName : site }}
    {% endif %}
{% endmacro %}

{% macro displayConsentStatus(value, time, ehrExpireStatus = '', ehrExpireDate = '') %}
    {% if ehrExpireStatus == 'EXPIRED' %}
        <i class="fa fa-exclamation-triangle text-warning" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Expired on {{ ehrExpireDate ? ehrExpireDate|date('n/j/Y', app.userTimezone) }})
    {% elseif value == 'SUBMITTED' %}
        <i class="fa fa-check text-success" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Consented Yes)
    {% elseif value == 'SUBMITTED_NO_CONSENT' %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Refused Consent)
    {% elseif value == 'SUBMITTED_NOT_SURE' %}
        <i class="fa fa-question text-warning" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Responded Not Sure)
    {% elseif value == 'SUBMITTED_INVALID' %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Invalid)
    {% else %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
        (Consent Not Completed) <br/>
    {% endif %}
{% endmacro %}

{% macro displayGenomicsConsentStatus(value, time) %}
    {% if value == 'SUBMITTED' %}
        <i class="fa fa-check text-success" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Consented Yes)
    {% elseif value == 'SUBMITTED_NO_CONSENT' %}
        <i class="fa fa-check text-success" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Refused Consent)
    {% elseif value == 'SUBMITTED_NOT_SURE' %}
        <i class="fa fa-check text-success" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Responded Not Sure)
    {% elseif value == 'SUBMITTED_INVALID' %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y', app.userTimezone) }} <br/>
        (Invalid)
    {% else %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
        (Consent Not Completed) <br/>
    {% endif %}
{% endmacro %}

{% macro displayConsentPanelStatus(value, time) %}
    {% set consentValues = ['SUBMITTED', 'SUBMITTED_NO_CONSENT', 'SUBMITTED_NOT_SURE', 'SUBMITTED_INVALID'] %}
    {% if value in consentValues %}
        {{ time ? time|date('n/j/Y g:ia', app.userTimezone) : '--' }}
    {% else %}
        (not completed)
    {% endif %}
{% endmacro %}

{% macro displayProgramUpdate(consentCohort, programUpdate, time) %}
    {% if consentCohort != 'COHORT_2' %}
        (Not Applicable)
    {% elseif programUpdate == 'SUBMITTED' %}
        <i class="fa fa-check text-success" aria-hidden="true"></i>
        {{ time ? time|date('n/j/Y g:ia', app.userTimezone) : '--' }}
    {% else %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
        (review not completed)
    {% endif %}
{% endmacro %}

{% macro displayFirstConsentPanelStatus(value, time, firstTime, type = 'primary') %}
    {% set consentValues = ['SUBMITTED', 'SUBMITTED_NO_CONSENT', 'SUBMITTED_NOT_SURE', 'SUBMITTED_INVALID'] %}
    {% if value in consentValues %}
        {% if value == 'SUBMITTED' %}
            {% set html = '<i class="fa fa-check text-success" aria-hidden="true"></i>' %}
        {% else %}
            {% set html = '<i class="fa fa-times text-danger" aria-hidden="true"></i>' %}
        {% endif %}
        {% if firstTime %}
            {{ firstTime|date('n/j/Y g:ia', app.userTimezone) }} {{ type == 'primary' ? '(first agreed to participate)' : '(first agreed to share EHR)' }} <br>
        {% endif %}
        {% if time %}
            {{ html|raw }} {{ time|date('n/j/Y g:ia', app.userTimezone) }} (current status)
        {% else %}
            --
        {% endif %}
    {% else %}
        (not completed)
    {% endif %}
{% endmacro %}
