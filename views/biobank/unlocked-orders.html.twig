{% extends 'base.html.twig' %}
{% block title %}Orders - Participant Review - {% endblock %}
{% block bodycontainer %}container-fluid{% endblock %}

{% macro displayDate(order, dateType) %}
    {% if order[dateType] %}
        {{ order[dateType]|date('n/j/Y g:ia', app.userTimezone) }}
    {% else %}
        <i class="fa fa-times text-danger" aria-hidden="true"></i>
    {% endif %}
{% endmacro %}

{% import _self as today %}

{% block body %}
    <div class="page-header">
        <h2><i class="fa fa-list" aria-hidden="true"></i> Participant Review</h2>
    </div>
    <ul class="nav nav-tabs">
        <li role="presentation"><a href="{{ path('biobank_todayOrders') }}">Today's Biobank Orders</a></li>
        <li role="presentation"><a href="{{ path('biobank_unfinalizedOrders') }}">Unfinalized Biobank Orders</a></li>
        <li role="presentation" class="active"><a href="{{ path('biobank_unlockedOrders') }}">Unlocked Biobank Orders</a></li>
        <li role="presentation"><a href="{{ path('biobank_ordersRecentModify') }}">Recently Modified Biobank Orders</a></li>
    </ul>
    <br />
    <div class="container">
        <table class="table table-striped table-bordered">
            <thead>
            <tr>
                {% if is_granted('ROLE_SCRIPPS') %}
                    <th>Participant ID</th>
                {% endif %}
                <th>Order ID</th>
                <th>Created</th>
                <th>Collected</th>
                <th>Processed</th>
                <th>Finalized</th>
            </tr>
            </thead>
            <tbody>
            {% for order in orders %}
                <tr>
                    {% if is_granted('ROLE_SCRIPPS') %}
                        <td>
                            <a href="{{ path('biobank_order', { biobankId: order.biobank_id, orderId: order.id }) }}">{{ order.participant_id }}</a>
                        </td>
                    {% endif %}
                    <td>
                        <a href="{{ path('biobank_order', { biobankId: order.biobank_id, orderId: order.id }) }}">{{ order.order_id }}</a>
                    </td>
                    <td data-order="{{ order.created_ts }}">
                        {{ today.displayDate(order, 'created_ts') }}
                    </td>
                    <td data-order="{{ order.collected_ts }}">
                        {{ today.displayDate(order, 'collected_ts') }}
                    </td>
                    <td data-order="{{ order.processed_ts }}">
                        {{ today.displayDate(order, 'processed_ts') }}
                    </td>
                    <td>
                        {% if order.oh_type != 'unlock' %}
                            {{ today.displayDate(order, 'finalized_ts') }}
                        {% else %}
                            <i class="fa fa-times text-danger" aria-hidden="true"></i>
                            <span class="badge">Unlocked</span>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
{% block pagejs %}
    <script>
        $(document).ready(function () {
            $('table').DataTable({
                order: [[1, 'desc']],
                pageLength: 25
            });
        });
    </script>
{% endblock %}
